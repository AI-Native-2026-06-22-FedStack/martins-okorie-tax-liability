# TaxPulse FastAPI Tax Engine environment
# Copy to `services/compute/.env` for local compute-only runs.

COMPUTE_DB_PATH=:memory:
JWT_ISSUER=taxpulse-api
JWT_AUDIENCE=taxpulse-clients
JWT_PUBLIC_KEY=
JWT_PUBLIC_KEY_KID=taxpulse-local-dev

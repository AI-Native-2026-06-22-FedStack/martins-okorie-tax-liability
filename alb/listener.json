{
  "// path": "alb/listener.json",
  "// note": "Routes /v1 to the Core Case Service target group. HTTPS cert ARN is supplied by the deployment environment.",
  "loadBalancerArn": "<taxpulse ALB arn>",
  "port": 443,
  "protocol": "HTTPS",
  "certificates": [
    {
      "certificateArn": "<taxpulse ACM certificate arn>"
    }
  ],
  "defaultActions": [
    {
      "type": "fixed-response",
      "fixedResponseConfig": {
        "statusCode": "404",
        "contentType": "application/json",
        "messageBody": "{\"error\":\"not_found\"}"
      }
    }
  ],
  "rules": [
    {
      "priority": 10,
      "conditions": [
        {
          "field": "path-pattern",
          "values": [
            "/v1/*"
          ]
        }
      ],
      "actions": [
        {
          "type": "forward",
          "targetGroupArn": "<taxpulse-api target group arn>"
        }
      ]
    }
  ]
}

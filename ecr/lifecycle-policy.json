{
  "// path": "ecr/lifecycle-policy.json",
  "// note": "Skeleton only. Two rules; the counts and tag selection are filled in Task 1.",
  "rules": [
    {
      "rulePriority": 1,
      "description": "keep the last N tagged images",
      "selection": {},
      "action": {
        "type": "expire"
      }
    },
    {
      "rulePriority": 2,
      "description": "expire untagged beyond the most recent one",
      "selection": {},
      "action": {
        "type": "expire"
      }
    }
  ]
}
